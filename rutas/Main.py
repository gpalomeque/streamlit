import streamlit as st

st.set_page_config(
        page_title="Rutas-CDMX",
          page_icon="✂️",
        layout="wide",
        initial_sidebar_state="expanded",
        )
st.title("Rutas de transporte en la CDMX")
st.markdown("""2. Hacer una pregunta indicando el nombre de la colección donde se espera encontrar el contexto necesario para generar una respuesta mediante la opción **search**.""")
st.divider()